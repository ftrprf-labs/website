{
  "name": "ftrlabs-invitation-manager",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "FTRLABS Invitation Manager — MVP for the MACULIS_FIRST_FIVE campaign",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server/index.mjs",
    "dev": "node --watch server/index.mjs",
    "test": "node --test tests/*.test.mjs",
    "test:comm": "node --test --test-concurrency=1 tests/*.test.mjs"
  },
  "dependencies": {
    "pg": "^8.23.0",
    "xlsx": "0.18.5"
  }
}
