node_modules/

# Never commit real invitation data — it contains PII (names, e-mail, mobile).
data/*.json
!data/.gitkeep

# Local environment / secrets
.env
.env.local

# Visuele regressie: baseline is bewijs en gaat mee, 'after' is een run-artefact.
tools/visual/after/

docs/studies/assets/frames/
